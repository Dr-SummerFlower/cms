# 路由 (router)

上级目录说明见 `../AGENTS.md`（src）。

## 职责

定义前端路由和受保护路由逻辑。

## 当前文件

- **index.tsx** — 路由表与 `<Routes>` 配置（含公开页、用户页、管理后台、检票员等）。
- **protected.tsx** — 登录/角色校验，包裹需要认证或特定角色的路由。

## 约定

- 新增页面时在此注册路由；需要登录或角色权限的用 `protected.tsx` 或等价守卫包裹。
- 路径与后端无强绑定，但命名与业务一致（如 `/admin/*`、`/inspector/*`）。
