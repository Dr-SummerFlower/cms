# ECDSA 模块 (ecdsa)

上级模块说明见 `../AGENTS.md`（src）。

## 职责

ECDSA 相关能力（如签名、验签），供其他模块（如票务核验）调用。

## 主要文件

- **ecdsa.module.ts** — 模块定义与导出。
- **ecdsa.service.ts** — 签名/验签等具体实现。

## 约定

- 密钥与算法配置通过配置或环境变量管理，不硬编码敏感信息。
- 与 tickets 等调用方保持接口稳定，变更时同步更新调用处。
