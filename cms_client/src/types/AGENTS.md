# 类型定义 (types)

上级目录说明见 `../AGENTS.md`（src）。

## 职责

集中维护前端用到的 TypeScript 类型、接口，与后端 DTO/实体命名与结构对齐。

## 当前约定

- **index.ts** — 汇总导出或主要类型定义。
- 类型命名与后端一致（如 Concert、Ticket、User），便于前后端协作。

## 约定

- 后端新增/修改 DTO 或响应结构时，在此同步更新类型。
- 避免在业务文件里手写与后端重复的接口定义，优先从本目录引用。
